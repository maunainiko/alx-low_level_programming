#include <stdio.h>
/**
*main - entry point
*Declaration: Print all digits of base ten
*Return: (0)
*/
int main(void)
{
int num;

for (num=0; num < 10; num++)
printf("%d", num);
printf("\n");
return (0);
}
