#include "main.h"

/**
 * swap_int - swap the values of two integers
 * @a: integer to be swapped
 * @b: integer to swap value
 *
 * Return: void if successful
 */

void swap_int(int *a, int *b)
{
	int c;

	*a = *b;
	*b = c;
}

